<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :fromtime %><br />
    <%= f.datetime_select :fromtime %>
  </div>
  <div class="field">
    <%= f.label :totime %><br />
    <%= f.datetime_select :totime %>
  </div>
  <div class="field">
    <%= f.label :review %><br />
    <%= f.check_box :review %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
